import React from "react";

const StudentsList: React.FC<{
  classNumber: number;
  division: number;
  fatherName: string;
  name: string;
  schoolId: string;
  _id: string;
}> = ({classNumber, division,fatherName,name,schoolId,_id}) => {
  return <div>studentsList</div>;
};

export default StudentsList;
 
